## LLM Post-Train

这个项目是有关大模型Post-train的实现方案，此前我已经完成了qwen3 0.3B的模型预训练，对诸如GQA，ROPE，RMSNorm， KV cache等llm基础组件和机制有了一定了解。但是预训练算力消耗太大，及时是0.3B的模型也训练了很久，并且效果并不是很好。考虑到大模型发展中，预训练越来越集中到头部企业或者课题组，后训练则成为普通开发者更有机会触及的阶段，所以我打算花一个月左右时间从0开始，复现主流小模型的post-train流程，我将完整记录整个过程，包含算力购买、文献、心得、trick等等。